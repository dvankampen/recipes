<%= form_for(@new_recipe) do |f| %>
  <% if @new_recipe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@new_recipe.errors.count, "error") %> prohibited this new_recipe from being saved:</h2>

      <ul>
      <% @new_recipe.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :ingredients %><br />
    <%= f.text_area :ingredients %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
